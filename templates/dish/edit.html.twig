{% extends 'base.html.twig' %}

{% block title %}Edit Dish{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Edit Dish</h1>
        <div class="card bg-dark text-light shadow-lg">
            <div class="card-body">
                {{ include('dish/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
        <div class="mt-4">
            <a href="{{ path('app_dish_index') }}" class="btn btn-secondary">Back to list</a>
            {{ include('dish/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
