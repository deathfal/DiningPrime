{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-12 text-center">
            <h1 class="display-4">Admin Dashboard</h1>
            <p class="lead">Manage users, content, and site settings from here.</p>
            <a class="btn btn-primary btn-lg mt-3" href="{{ path('app_home') }}">Go to Home</a>
            <a class="btn btn-warning btn-lg mt-3" href="{{ path('app_user_index') }}">Manage Users</a>
        </div>
    </div>
</div>
{% endblock %}
